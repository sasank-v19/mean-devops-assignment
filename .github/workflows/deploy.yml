name: DevOps CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend Image
        run: |
          docker build -t sasi777/mean-backend:latest ./backend
          docker push sasi777/mean-backend:latest

      - name: Build Frontend Image
        run: |
          docker build -t sasi777/mean-frontend:latest ./frontend
          docker push sasi777/mean-frontend:latest